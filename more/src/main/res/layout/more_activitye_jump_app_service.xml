<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".component.JumpAppServiceActivity"
        android:background="@color/white"
        >

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="测试当前进程内的service服务"
            android:textColor="@color/black"
            android:textSize="16dp"
            android:gravity="center"
            app:layout_constraintTop_toTopOf="parent"
            android:layout_marginTop="30dp"
            android:id="@+id/tv_cur_process_service_activity"
            />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="stopService"
            android:textColor="@color/black"
            android:textSize="16dp"
            android:gravity="center"
            app:layout_constraintTop_toBottomOf="@id/tv_cur_process_service_activity"
            android:layout_marginTop="30dp"
            android:id="@+id/tv_stop_service"
            />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>